import React from 'react'
import styled from 'styled-components'

const Container = styled.div`
  padding: var(--spacing-v1);
`

const Ads = () => {
  return <Container>publicidad</Container>
}

export default Ads
